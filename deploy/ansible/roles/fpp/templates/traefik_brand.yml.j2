---
http:
  routers:
    send-push-{{ item.key }}:
      entryPoints:
        - http
      middlewares:
        - fpp-auth
        - remove-auth-header
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/send`)
      service: fpp-{{ item.key }}
    send-push-https-{{ item.key }}:
      entryPoints:
        - https
      middlewares:
        - fpp-auth
        - remove-auth-header
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/send`)
      service: fpp-{{ item.key }}
      tls: {}
    register-{{ item.key }}:
      entryPoints:
        - http
      middlewares:
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/register`)
      service: fpp-{{ item.key }}
    register-https-{{ item.key }}:
      entryPoints:
        - https
      middlewares:
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/register`)
      service: fpp-{{ item.key }}
      tls: {}
    deregister-{{ item.key }}:
      entryPoints:
        - http
      middlewares:
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/deregister`)
      service: fpp-{{ item.key }}
    deregister-https-{{ item.key }}:
      entryPoints:
        - https
      middlewares:
        - strip-brand-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/deregister`)
      service: fpp-{{ item.key }}
      tls: {}
    metrics-{{ item.key }}:
      entryPoints:
        - http
      middlewares:
        - strip-brand-metrics-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/{{ fpp_metrics_path }}/metrics`)
      service: fpp-{{ item.key }}
    metrics-https-{{ item.key }}:
      entryPoints:
        - https
      middlewares:
        - strip-brand-metrics-{{ item.key }}
      rule: PathPrefix(`/{{ item.key }}/{{ fpp_metrics_path }}/metrics`)
      service: fpp-{{ item.key }}
      tls: {}
  middlewares:
    strip-brand-{{ item.key }}:
      stripPrefix:
        prefixes:
          - "/{{ item.key }}/"
    strip-brand-metrics-{{ item.key }}:
      stripPrefix:
        prefixes:
          - "/{{ item.key }}/{{ fpp_metrics_path }}/"
  services:
    fpp-{{ item.key }}:
      loadBalancer:
        passHostHeader: false
        servers:
          - url: 'http://localhost:{{ item.value.http_port }}'

